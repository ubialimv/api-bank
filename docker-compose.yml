version: '3.9'

services:
  api-bank-db:
    image: postgres
    environment:
      POSTGRES_PASSWORD: 'postgres'
      POSTGRES_DB: 'bank'
    ports:
      - '15432:5432'
    volumes:
      - api-bank-data:/var/lib/postgresql/data

volumes:
  api-bank-data:
